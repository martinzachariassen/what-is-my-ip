{
  "name": "functions",
  "private": true,
  "description": "Cloud Function backing /ip on ip.mlz.no — echoes the caller's real address.",
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "_comment": "firebase-admin is not imported by index.js, but it is a required (non-optional) peer of firebase-functions and gets installed either way — pinned here rather than left to peer resolution.",
  "dependencies": {
    "firebase-admin": "^14.3.0",
    "firebase-functions": "^7.3.2"
  }
}
